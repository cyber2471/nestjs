# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DB_HOST="140.238.21.87"
DB_NAME="astems"
DB_USER="neoib"
DB_PASSWORD="neoib0"
DB_SCHEMA="nestjs"

# # mysql
DB_HOST="localhost"
DB_TYPE="mysql"
DB_PORT="3306"
DB_USER="cyberpark"
DB_PASSWORD="cyberpark"

# sqlserver
# DB_HOST="localhost"
# DB_TYPE="sqlserver"
# DB_PORT="1433"
# DB_USER="cyberpark"
# DB_PASSWORD="cyberpark"

# # redis
# DB_TYPE="redis"
# DB_PORT="6379"

# postgresql 9069 5432
#DATABASE_URL="${DB_TYPE}://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?schema=${DB_SCHEMA}"

# sqlserver 1433
### DATABASE_URL="${DB_TYPE}://${DB_HOST}:${DB_PORT};database=${DB_NAME};integratedSecurity=true;username=${DB_USER};password=${DB_PASSWORD};trustServerCertificate=true"
# DATABASE_URL="${DB_TYPE}://${DB_HOST}:${DB_PORT};database=${DB_NAME};username=${DB_USER};password=${DB_PASSWORD};trustServerCertificate=true"
## DATABASE_URL="${DB_TYPE}://${DB_HOST}:${DB_PORT};database?integratedSecurity=true"
# 명명된 인스턴스:SQLExpress

# mongodb 27017 
#DATABASE_URL="${DB_TYPE}://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# mysql 3306
DATABASE_URL="${DB_TYPE}://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# # redis 6379
# DATABASE_URL="${DB_TYPE}://${DB_HOST}:${DB_PORT}